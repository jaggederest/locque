# Test that an imported module can reference its own computations

import prelude as P
import assert as A

module helper contains
  define transparent helper-comp as computation
    bind perform io (A.assert-eq-nat 5 5) as _ then
    return P.tt

  define transparent main as computation
    bind helper-comp as _ then  # Self-reference to helper-comp
    return P.tt
end
