# Test runner for value references

import test::test_imported_value_ref as helper
import prelude as P
import assert as A

module test_value_runner contains
  define transparent main as computation
    bind perform io (A.assert-eq-nat (helper.use-helper 5) 15) as _ then
    return P.tt
end
