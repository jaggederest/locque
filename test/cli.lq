import prelude as P
# Test rollup for cli module
# Runs all tests in test/cli/

import test::cli::basics as basics

module test::cli contains
  define transparent main as compute
    perform (P::sequence-unit
      [
        basics::main
      ])
  end
end
