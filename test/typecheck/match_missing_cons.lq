# Expected type error: missing cons-case.

import prelude as P

module test::typecheck::match_missing_cons contains
  define transparent bad-match as
    match (P::nil Natural) of-type (List Natural) as ignored returns Natural
      empty-case as 0
    end

  define transparent main as compute
    return tt
  end
end
