# Expected type error: unexpected case Boolean::true.

import prelude as P

module test::typecheck::match_unexpected_case contains
  define transparent bad-match as
    match (of-type [] (List Natural)) of-type (List Natural) as ignored returns Natural
      case List::empty as 0
      case List::cons with h Natural t (List Natural) as h
      case Boolean::true as 1
    end

  define transparent main as compute
    return tt
  end
end
