# Main test entry point
# Runs only the prelude basics rollup

import test::prelude as test_prelude

module test::main contains
  define transparent main as compute
    bind ignored from perform test_prelude::main then
      return tt
    end
  end
end
