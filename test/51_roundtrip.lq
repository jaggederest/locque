import Prelude as P

module Roundtrip contains

  define transparent greet as value
    function name of-type String produce concat-string-prim "hi " name

  define transparent choose as computation
    P.match-list P.nil
      (lambda () "empty")
      (lambda h -> lambda t -> h)

  define transparent main as computation
    return P.tt
end
