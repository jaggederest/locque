# Test rollup for Assert module

import assert as A
open A exposing assert-eq Equality end

module test::assert contains
  define transparent main as compute
    sequence
      (assert-eq Natural 42 42)
      (assert-eq String "locque" "locque")
      (assert-eq Boolean false false)
      (assert-eq Natural 2 2)
      (assert-eq String "hello" "hello")
      (assert-eq Boolean true true)
    end
  end
end
