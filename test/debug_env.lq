# Minimal test to debug environment capture

import prelude as P

module debug_test contains
  define transparent helper as value
    function x of-type Nat produce x

  define transparent use-helper as value
    function y of-type Nat produce
      helper y  # Should find 'helper' in environment

  define transparent main as computation
    return P.tt
end
