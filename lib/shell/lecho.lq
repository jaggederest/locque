import io as IO
import prelude as P

module shell::lecho contains
  define transparent lecho as
    function s String returns computation String value
      P::tap String IO::print (compute return s end)
    end
end
