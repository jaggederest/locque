#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BIN="$ROOT/tmp/locque/bin/smyth"

if [[ ! -x "$BIN" ]]; then
  echo "Locque smyth binary not found. Run: $ROOT/bin/build-locque-smyth" >&2
  exit 1
fi

exec "$BIN" "$@"
